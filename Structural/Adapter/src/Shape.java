// Legacy code, for which adapter will be created. Allowing the user to simply use the create function as defined in the legacy code without worrying about the objects shape.
public interface Shape {
	void create(int x0, int y0, int x1, int y1);
}
